import AboutUs from '../components/AboutUs'; // Asegúrate que la ruta sea correcta

export default function AboutPage() {
    // El componente AboutUs se envuelve automáticamente con el Layout
    // definido en tu archivo layout.tsx
    return <AboutUs />;
}